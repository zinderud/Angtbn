/*import { TbnPage } from './app.po';

describe('tbn App', () => {
  let page: TbnPage;

  beforeEach(() => {
    page = new TbnPage();
  });

  it('should display message saying app works', () => {
    page.navigateTo();
    expect(page.getParagraphText()).toEqual('tbn works!');
  });
});
*/